page {
  height: 100vh;
  overflow: hidden;
  color: #111111;
  background:
    radial-gradient(circle at 14% 0%, rgba(221, 198, 161, 0.18), transparent 34%),
    linear-gradient(180deg, #fbf5ec 0%, #f8f0e6 100%);
  font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", Arial, sans-serif;
}

.settings-page {
  height: calc(100vh - 88px);
  overflow: hidden;
  padding: 56rpx 30rpx 80rpx;
  box-sizing: border-box;
}

.setting-section {
  margin-bottom: 34rpx;
}

.section-title {
  margin: 0 0 12rpx 14rpx;
  color: #7f7973;
  font-size: 26rpx;
  font-weight: 500;
  line-height: 36rpx;
}

.setting-card {
  overflow: hidden;
  border: 2rpx solid #d4c8b8;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.72);
  box-sizing: border-box;
}

.setting-row {
  min-height: 86rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin: 0 44rpx;
  border-bottom: 1rpx solid #d7cec2;
  color: #1f1b18;
  font-size: 28rpx;
  font-weight: 500;
  box-sizing: border-box;
}

.setting-row:last-child {
  border-bottom: 0;
}

.row-arrow {
  width: 40rpx;
  height: 40rpx;
  flex: none;
  position: relative;
}

.row-arrow::before {
  content: "";
  position: absolute;
  left: 9rpx;
  top: 7rpx;
  width: 20rpx;
  height: 20rpx;
  border-right: 4rpx solid #b4aea8;
  border-top: 4rpx solid #b4aea8;
  transform: rotate(45deg);
  box-sizing: border-box;
}

.tap-active {
  opacity: 0.68;
}

.dialog-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(44, 36, 28, 0.28);
  z-index: 100;
}

.clear-dialog {
  width: 600rpx;
  overflow: hidden;
  border-radius: 24rpx;
  background: #fffdf9;
  box-shadow: 0 18rpx 42rpx rgba(44, 36, 28, 0.2);
}

.account-confirm-dialog {
  min-height: 286rpx;
  display: flex;
  flex-direction: column;
}

.account-confirm-dialog .dialog-text {
  min-height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.account-confirm-dialog .dialog-actions {
  margin-top: auto;
}

.dialog-title {
  padding: 38rpx 42rpx 16rpx;
  color: #111111;
  font-size: 34rpx;
  font-weight: 700;
  text-align: center;
}

.dialog-text {
  padding: 0 46rpx 36rpx;
  color: #6f6861;
  font-size: 26rpx;
  line-height: 40rpx;
  text-align: center;
}

.dialog-actions {
  display: flex;
  border-top: 1rpx solid #eadfce;
}

.dialog-actions.single .dialog-button {
  border-left: 0;
}

.dialog-button {
  flex: 1;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 29rpx;
  font-weight: 600;
}

.dialog-button + .dialog-button {
  border-left: 1rpx solid #eadfce;
}

.dialog-button.cancel {
  color: #6f6861;
}

.dialog-button.confirm {
  color: #c52e2b;
}

.dialog-button.confirm.disabled {
  opacity: 0.45;
}

.success-confirm {
  color: #1f1b18;
}

.account-loading-toast {
  position: fixed;
  left: 50%;
  top: 50%;
  min-width: 232rpx;
  min-height: 232rpx;
  padding: 28rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  color: #8b6840;
  border-radius: 16rpx;
  background: rgba(255, 253, 249, 0.96);
  box-shadow: 0 16rpx 38rpx rgba(85, 67, 48, 0.12);
  font-size: 30rpx;
  font-weight: 500;
  line-height: 40rpx;
  transform: translate(-50%, -50%);
  z-index: 120;
  box-sizing: border-box;
}

.loading-gift-icon {
  width: 72rpx;
  height: 72rpx;
  display: block;
  animation: loading-gift-bounce 900ms ease-in-out infinite;
}

@keyframes loading-gift-bounce {
  0%,
  100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-10rpx);
  }
}
